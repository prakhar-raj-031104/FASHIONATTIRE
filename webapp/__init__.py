"""Optional web demo portal for the fashion retrieval system.

Not part of the assessed ML pipeline — it is a thin Flask layer over the same Retriever
used by the CLI, so what you see in the browser is exactly what the system returns.
"""
